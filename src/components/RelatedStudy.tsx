import React, {Dispatch} from 'react';
import {StudyDetails} from "../study";

interface Props {
  onReturn?: Dispatch<void>,
  studyDetails: StudyDetails,
}

export function RelatedStudy({onReturn}: Props) {
  return <div>
  </div>
}